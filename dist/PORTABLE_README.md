# Portable Auto Clicker

Copy `autoclicker.exe` and `clicker_settings.json` together to any writable folder, then run the executable.

The settings file is kept beside the executable. Do not place the folder in a protected location such as `C:\Program Files`.
